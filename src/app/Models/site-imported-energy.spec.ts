import { SiteImportedEnergy } from './site-imported-energy';

describe('SiteImportedEnergy', () => {
  it('should create an instance', () => {
    expect(new SiteImportedEnergy()).toBeTruthy();
  });
});
