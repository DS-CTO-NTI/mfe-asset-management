import { MemsDeviceStatusCode } from './mems-device-status-code';

describe('MemsDeviceStatusCode', () => {
  it('should create an instance', () => {
    expect(new MemsDeviceStatusCode()).toBeTruthy();
  });
});
