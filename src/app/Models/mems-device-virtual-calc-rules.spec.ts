import { MemsDeviceVirtualCalcRules } from './mems-device-virtual-calc-rules';

describe('MemsDeviceVirtualCalcRules', () => {
  it('should create an instance', () => {
    expect(new MemsDeviceVirtualCalcRules()).toBeTruthy();
  });
});
