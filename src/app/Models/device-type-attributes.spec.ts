import { DeviceTypeAttributes } from './device-type-attributes';

describe('DeviceTypeAttributes', () => {
  it('should create an instance', () => {
    expect(new DeviceTypeAttributes()).toBeTruthy();
  });
});
