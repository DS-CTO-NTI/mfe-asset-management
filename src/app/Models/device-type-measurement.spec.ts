import { DeviceTypeMeasurement } from './device-type-measurement';

describe('DeviceTypeMeasurement', () => {
  it('should create an instance', () => {
    expect(new DeviceTypeMeasurement()).toBeTruthy();
  });
});
