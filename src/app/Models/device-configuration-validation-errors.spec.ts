import { DeviceConfigurationValidationErrors } from './device-configuration-validation-errors';

describe('DeviceConfigurationValidationErrors', () => {
  it('should create an instance', () => {
    expect(new DeviceConfigurationValidationErrors()).toBeTruthy();
  });
});
