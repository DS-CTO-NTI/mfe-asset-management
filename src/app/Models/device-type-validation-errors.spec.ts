import { DeviceTypeValidationErrors } from './device-type-validation-errors';

describe('DeviceTypeValidationErrors', () => {
  it('should create an instance', () => {
    expect(new DeviceTypeValidationErrors()).toBeTruthy();
  });
});
